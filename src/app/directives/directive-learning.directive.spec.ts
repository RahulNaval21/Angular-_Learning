import { DirectiveLearningDirective } from './directive-learning.directive';

describe('DirectiveLearningDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveLearningDirective();
    expect(directive).toBeTruthy();
  });
});
